"use client";

// Once rollup supports 'use client' module directives, then next-sanity will include them in the bundle and this re-export will be unnecessary.
export { PreviewSuspense as default } from "next-sanity/preview";